<?xml version="1.0" encoding="utf-8" ?>
<nlog xmlns="http://www.nlog-project.org/schemas/NLog.xsd"
      xmlns:xsi="http://www.w3.org/2001/XMLSchema-instance"
      autoReload="true"
      throwExceptions="false">

  <targets>
    <target name="console"
            xsi:type="ColoredConsole"
            layout="${date:format=HH\:mm\:ss} ${level:uppercase=true:padding=-5} ${logger:shortName=true} ${message}${onexception:${newline}${exception:format=tostring}}" />

    <target name="file"
            xsi:type="File"
            fileName="C:\temp\${processname}\app_${cached:${date:format=yyyyMMdd_HHmmss}}.log"
            layout="${longdate} ${level:uppercase=true:padding=-5} ${logger} ${message}${onexception:${newline}${exception:format=tostring}}" />
  </targets>

  <rules>
    <logger name="*" minlevel="Info"  writeTo="console" />
    <logger name="*" minlevel="Warn"  writeTo="file" />
  </rules>

</nlog>
